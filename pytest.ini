[pytest]
markers =
    e2e: fast end-to-end test with mocked LLM clients (safe for CI, runs in seconds)
    e2e_real: full end-to-end test with REAL LLM calls (slow, costs API credits, opt-in only)
    slow: long-running test, excluded from default runs
testpaths = tests
